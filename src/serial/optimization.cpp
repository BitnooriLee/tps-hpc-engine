// Optimization formulas are all header-inline (include/tps/optimization.hpp).
// This TU exists as a build target placeholder and for future non-trivial
// implementations (e.g. choose_best_cp with R² sweep).
